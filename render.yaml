services:
  - type: web
    name: fastapi-backend
    runtime: python
    rootDir: backend
    buildCommand: |
      pip install --upgrade pip
      pip install -r requirements.txt
      prisma generate
      prisma db push
      prisma py fetch
    startCommand: prisma py fetch 
                  uvicorn app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: python
        value: python3.10
      - key: DATABASE_URL
        fromDatabase:
          name: my-postgres-db
          property: connectionString
    plan: free

  - type: web
    name: react-frontend
    env: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: build
    routes:
      - type: rewrite
        source: /api/*
        destination: https://fastapi-backend-irwb.onrender.com/api/*

databases:
  - name: my-postgres-db
    plan: free
